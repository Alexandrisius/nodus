import { cn } from '@nodus/ui/lib/utils';

/**
 * Хвостик пузыря — ЧАСТЬ ЕДИНОГО СИЛУЭТА (вердикты владельца 14.09.2026).
 * Техника: НИКАКИХ линий и обводок (стык CSS-бордюра пузыря и SVG-обводки
 * антиалиасился по-разному и давал «полутонкие линии» и швы) — пузыри чата
 * без рамки (вариант `card`, контур ступенью тона, рефы Битрикс24), хвост —
 * ОДНА заливка цветом пузыря, силуэт которой сам содержит вогнутый край
 * (дуга r=8 от кромки пузыря к кончику) и ЗАКРУГЛЁННЫЙ носик (дуга r=2.2 —
 * вердикт владельца «носик слишком острый»: не игла, а мягкий закруглённый
 * кончик, как в рефах). Кончик упирается в нижний угол аватарки, не заходя
 * на него (левый край плавника правее края аватара на 0.3px). Угол пузыря
 * со стороны хвоста — rounded-none: плавник накрывает прямой угол, силуэт
 * идёт одной кривой. Только у ПОСЛЕДНЕГО пузыря серии.
 */
const PATHS = {
  /** Силуэт (вердикт владельца 14.09.2026 «не закорючка, аккуратный как в
   *  Битриксе»): ТОЛСТЫЙ плавник высотой ~9px, ПОЛОГАЯ вогнутая кривая от
   *  кромки пузыря (вход в кромку почти вертикально — силуэт непрерывен) и
   *  мягкий закруглённый носик-пуговка (Q-дуга), кончик на 0.5px правее
   *  края аватарки — не заходит на него. Левый край пузыря — svg x=12. */
  left: 'M12 0 H20 V20 H12 L6.8 20 Q4.2 19.6 5.5 18.5 C8.5 17.5 11.5 15.5 12 11 Z',
  right: 'M8 0 H0 V20 H8 L13.2 20 Q15.8 19.6 14.5 18.5 C11.5 17.5 8.5 15.5 8 11 Z',
} as const;

export function BubbleTail({
  side,
  variant,
}: {
  side: 'left' | 'right';
  variant: 'default' | 'card';
}) {
  return (
    <svg
      aria-hidden
      viewBox="0 0 20 20"
      className={cn(
        'absolute bottom-0 size-[1.25rem]',
        side === 'left' ? '-left-[0.75rem]' : '-right-[0.75rem]',
      )}
    >
      <path d={PATHS[side]} className={variant === 'card' ? 'fill-card' : 'fill-primary'} />
    </svg>
  );
}
