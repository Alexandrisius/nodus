# План UI-переделки шелла (вердикты владельца 12–14.09.2026, финальный заход)

**Статус:** план РЕАЛИЗОВАН 14.09.2026 на ветке `feat/4-node-ui` (фазы 1–4, коммиты `056a91f`, `57b57cd`, `0efe516` + gotchas-коммит); документ оставлен как карта требований и механизмов для будущих правок шелла.
**База:** ветка `feat/4-node-ui`, HEAD = `464eb15` (UI образца 12–13.09, владелец назвал его «мой UI»).
**Трекер:** issue #4 (эпик node-ui); одна фаза = один коммит с `(#4)`.
**Среда:** dev-сервер `pnpm dev` на localhost:5173 (закладка владельца); владелец проверяет ВЖИВУЮ обе темы, скриншоты ему НЕ слать («не делай кадры, я сам смотрю»); валидацию субагентом НЕ запускать («валидацию не запускай больше»).
**Читатель плана:** новый агент после суммаризации контекста. Всё нужное — здесь + указанные хеши коммитов (они остались в истории git, ветка переписана force-push, но объекты живы: доставать через `git show <hash> -- <path>`).

## Контекст владельца (коротко)

Владелец — нетехнический product owner; его слово истина в визуальных вердиктах. За 12–14.09 прошли четыре круга правок; последние эксперименты (14.09: контурный спуск-граница колонки, скроллбар в щели периметра, шелл-скроллер со sticky-шапкой, border-b топбара, плашка/ободок-плашка профиля, чипы канбана, gap-8 у поиска) владелец ОТКЛОНИЛ целиком и откатил всё к `464eb15`. Ниже — только то, что он подтвердил словами «100 процентов нужно / точно нужно / хочу».

## Требования (формулировки владельца → техническая суть)

- **R1 (100%):** убрать вертикальную границу левой рейки И разность тона рейки с периметром — левая сторона портала общая с периметром, как в рефе `docs/mvp/ref/node-based UI/Задачи_Светлая тема.jpg` (воздушность).
- **R2 (точно):** сортировка списка сотрудников/чатов по активности диалогов + чат «Заметки» (сообщения себе, модель Битрикс24: закладка вместо своей аватарки, свой профиль не дублируется в коллегах).
- **R3 (точно):** уменьшить аватарку профиля до кегля аватарок сотрудников (size-7) и выровнять по ним (та же колонка/ось центровки).
- **R4 (новая идея):** служебная полоса (главный профиль + аватарки сотрудников) — ЗА ПРЕДЕЛАМИ мягкой области (в правом периметре). Вертикальную границу правой полосы НЕ делать: раскрытие (КНОПКОЙ-шевронами внизу полосы; без авто-раскрытия по наведению и с персистом выбора — вердикт владельца вечером 14.09.2026) происходит РЕФЛОУ — мягкая область сдвигается/сужается влево, полоса расширяется; то же для карточек сущностей (не накрывают полосу, сужаются вместе с рамой). В раскрытой полосе строка профиля подписана «Мой профиль». Ширина раскрытия ≈ 240px − 20% ≈ 192px.
- **R5:** ось контура доходит только до границы мягкой области и не пересекает зону аватарок. Вердикт вечером 14.09.2026: вне мессенджера ось обрезается до ПОСЛЕДНЕГО подмодуля — аккуратной дугой в его засечку, без хвостов и зазоров (ошибка прошлого раунда — торчащие хвостики); в мессенджере ось остаётся на всю ширину рамы как верхняя граница окна чата; на Главной (подмодулей нет) связь доводится до ЛЕВОГО КРАЯ мягкой области и заканчивается на нём терминальной точкой; засечки вкладок — локти со скруглённым входом в ось (голая вертикаль = «прямоугольный угол» рядом со скруглением вспышки).
- **R6:** главную аватарку профиля выделить лёгким тонким цветом в нашем стиле (тонкий ободок, без плашек и яркости).
- **R7 (подтверждено ранее, вернуть):** меню профиля и все оверлей-порталы/тосты — ПОВЕРХ служебной полосы (аватарки просвечивали в меню «Выйти»).
- **R8 (заменено вердиктом владельца вечером 14.09.2026):** авто-раскрытия по наведению НЕТ вовсе — раскрытие кнопкой-шевронами внизу полосы (как у левой рейки), выбор персистится; наведения не раскрывают ничего.
- **R9 (подтверждено ранее, вернуть):** плавное выдвижение/скрытие полосы (~200 мс), без рывков.
- **R10 (подтверждено ранее, вернуть):** переход в только что созданные «Заметки» не открывает пустой мессенджер (инвалидация кэша списка бесед).

ВНЕ скоупа (владелец в финальный список НЕ включил; не делать без его слова): структурные линии шапки, чипы канбана, зазоры поиска, кнопка «Создать», дефолт темы (отдельное решение владельца), мерж PR #4 (ждёт его «ок»). Скроллбар реализован ОТДЕЛЬНЫМ вердиктом ночью 14.09.2026: собственный ползунок в правой щели периметра, трек на всю высоту экрана (`edge-scrollbar`).

## Фаза 1 — воздушность левой стороны (R1)

Достать из `88266de` и `d533e71`:
1. `apps/web/src/app/shell/node-rail.tsx`: убрать `border-r border-sidebar-border` у aside рейки.
2. `packages/ui/src/styles/globals.css`: `--sidebar` = значению `--background` в ОБЕИХ темах (dark `#0a0a0b`, light `#f4f2f0`) + комментарий-вердикт; структуру рейки несут плашки (`sidebar-accent`), не ступень тона.
3. Узел схлопнутой рейки: якорь `data-left-node` — на ЛЕВОМ КРАЕ мягкой области (`-right-[8.5px]`, центр на оси 63px+1), точку рисует svg контура кругом (DOM-точка резалась overflow-hidden рейки) — из `88266de`.
Приёмка: computed `background-color` aside рейки === background body; `border-right-width` = 0; на Главной и Задачах визуально одна подложка слева и по периметру (обе темы).

## Фаза 2 — служебная полоса вне мягкой рамы, рефлоу-раскрытие (R3, R4, R5, R6, R8, R9)

Новая геометрия шелла (`apps/web/src/app/shell/app-shell.tsx`):
1. Средняя колонка = flex-row: [мягкая рама `flex-1` с отступами 8px сверху/слева/снизу и БЕЗ правого отступа] + [служебная полоса: `w-10` в свёрнутом виде, `margin-right: 8px` (щель периметра справа сохраняется), на всю высоту вьюпорта, фон прозрачный (тон периметра), БЕЗ бордюров (R4)].
2. Рама = `data-soft-frame` как сейчас; её правый край = левый край полосы. Контур: `rightEdge` измеряется от rect рамы → ось сама остановится на границе мягкой области (R5) — в `464eb15` ось уже меряется от `data-soft-frame`, правок контура не нужно, только проверить пробой.
3. Полоса сверху вниз: ячейка профиля h-14 (R3: `ProfileMenu` с аватаркой size-7 по центру полосы — точно над аватарками коллег; достать раскладку ячейки из `4d77da8`), ниже список: «Заметки» + коллеги (Фаза 3). Dwell-триггер — ТОЛЬКО список и раскрытая панель (R8); ячейка профиля вне триггера.
4. Раскрытие РЕФЛОУ (R4): ширина полосы анимируется `w-10` → `w-48` (192px = 240 − 20%); рама `flex-1` сужается сама; transition width ~200 мс ease-out (R9). Контур перемеряется сам: в `circuit-frame.tsx` уже есть покадровый пересчёт на transition `propertyName === 'width'` (transitionrun/transitionend) — НЕ ломать этот механизм. Раскрытая панель: сверху строка «Мой профиль» (аватарка + подпись, строка i18n `ui.topbar.myProfile = 'Мой профиль'` в `packages/contracts/src/i18n/ru.ts`), ниже «Заметки» и коллеги с именами. Никаких overlay/invisible/translate: чистый рефлоу — уходят все прошлые gotchas клиппинга.
5. Карточки сущностей (ADR-0009, `slider-panel.tsx`): правый край карточки = правый край рамы в ОБОИХ состояниях полосы. Реализация: right-inset карточки = `calc(ширина полосы + 8px)` через CSS-переменную/inline-стиль от текущего состояния + тот же transition width (карточка сужается синхронно с рамой); FLIP-геометрию раскрытия (`dst`) считать НЕ из констант window, а замером rect `data-soft-frame` в момент открытия (gotcha: константы протухли с динамической шириной рамы). Click-catcher не должен перехватывать полосу (модель Битрикс24: колонка рабочая поверх карточки).
6. R6: тонкий ободок главной аватарки — `ring-1 ring-primary/60` на `PersonAvatar` профиля (достать из `c649503`); никаких плашек/колец толщины >1px (владелец отверг «толстый обруч»).
7. Лестница z (R7): оверлей-порталы и тосты = z-[70] выше полосы (полоса z-[60], слайдеры z-50): правки `packages/ui/src/components/{dropdown-menu,dialog,popover,select,sheet,tooltip,context-menu}.tsx` (z-50 → z-[70]) и `sonner.tsx` (zIndex 70) — достать из `f559f6a`.
Приёмка: rect полосы right === innerWidth − 8; rect рамы right === rect полосы left; после нажатия кнопки раскрытия: ширина полосы ≈192 и ширина рамы уменьшилась на ту же дельту; открытая карточка задачи: right === right рамы в свёрнутом и раскрытом состояниях; в раскрытой полосе видна подпись «Мой профиль»; наведение на список и профиль ширину полосы не меняет (авто-раскрытия нет); состояние раскрытия переживает reload; выдвижение плавное (семплы computed width по 100 мс).

## Фаза 3 — «Заметки» и сортировка по активности (R2, R10)

Достать целиком из `f559f6a` (когерентный набор):
1. `apps/web/src/shared/ui/notes-glyph.tsx` — закладка в круге muted.
2. `features/chat/lib/conversations.ts`: `isNotesConversation()` + `conversationTitle(c, meId)`; `features/chat/components/conversation-list.tsx` и `features/chat/pages/chat-page.tsx`: глиф и заголовок «Заметки» вместо своей аватарки/имени; i18n `ui.chat.notes = 'Заметки'`.
3. Полоса (компонент из Фазы 2): строка «Заметки» первой в списке; коллеги БЕЗ себя; сортировка по `lastMessage.createdAt`.direct-диалогов из `useConversations()` (убывание, без диалога — в конце, стабильно); серверная сортировка — заметка на будущее бэкенду (контракт `lastMessage` уже есть).
4. Переход в «Заметки»: `useDirectConversation(me.id)` (find-or-create) + `invalidateQueries(chatKeys.conversations())` ДО navigate (R10, gotcha staleTime 30s).
Приёмка: первая строка полосы — закладка; имени «Александр Климович» среди коллег нет; порядок коллег = порядку lastMessage демо-чатов; клик по «Заметкам» → `/chat/<id>` с заголовком «Заметки» и глифом в шапке и списке.

## Фаза 4 — зеркала, гейты, приёмка

1. Зеркала канона в тех же коммитах (I12): `.agents/skills/nodus-ui-style/SKILL.md` §3 (блок мягкости: полоса вне рамы, рефлоу-раскрытие 192px, «Мой профиль», ободок ring-1, ось до края рамы, тон рейки = периметр), `references/circuit.md` (rightEdge = правый край рамы; точка схлопнутой рейки на левом крае), `docs/adr/0009-entity-card-stack.md` (правый inset карточки = полоса + 8, FLIP по замеру рамы), README затронутых модулей.
2. Gotchas (`docs/gotchas.md`) дополнить механизмами, подтверждёнными в этих раундах: (а) Tailwind v4: `translate-x-*` анимирует CSS-свойство `translate`, `transition-transform` в v4 его покрывает, а произвольный `transition-[transform,…]` без translate даёт рывок; (б) flex-рама в скроллере: flex-item сжимается shrink'ом до min-h и контент выливается наружу — `shrink-0` при потоковом росте; (в) staleTime 30s: инвалидация списка бесед до navigate в созданный find-or-create диалог; (г) временные probe-файлы в `tests/e2e/` удалять ДО коммита: lint-staged гоняет eslint по staged и падает на no-undef.
3. Гейты каждого коммита: `pnpm lint`, `pnpm typecheck`, `pnpm --filter @nodus/web test`, `pnpm build` (известные 4 warning I5 — норма).
4. Headless-пробы (Playwright, `chromium` из `@playwright/test`, viewport 1600×900, msw-мок авторизован) — по пунктам «Приёмка» каждой фазы; ключевые замер-точки: `getComputedStyle(aside рейки).backgroundColor/background/borderRightWidth`; rect'ы `[data-soft-frame]`, полосы, `aside.w-10 div.w-48`; `document.querySelector('svg path[stroke="var(--foreground)"]').getAttribute('d')` (конец оси = right рамы ±1); `elementsFromPoint` в центре меню профиля; таймлайн ширины полосы по 100 мс после нажатия кнопки раскрытия.
5. После каждой фазы — живой просмотр владельцем обеих тем; не смешивать фазы в одном коммите.

## Отклонённые эксперименты (НЕ повторять без нового слова владельца)

Контурный вертикальный спуск как граница колонки (торчал из-за карточки и у её мягкого угла); скроллбар в щели периметра и шелл-скроллер со sticky-шапкой/колонкой («не доходит до конца, стало хуже»); `border-b` топбара как структурная линия; плашка вокруг аватарки профиля («толстый обруч»); оверлей-раскрытие полосы поверх контента (заменено рефлоу, R4); нейтрализация чипов канбана и gap-8 у поиска (не согласованы); любые горизонтальные линии контура в шапке длиннее связи модуль→подмодуль («хвостик», «убрать линию связи»).

## Карта доставки из истории git (объекты живы после force-push)

- `88266de` — снятие border-r рейки, якорь узла на левом крае рамы, svg-точка.
- `d533e71` — токены `--sidebar = --background` (обе темы).
- `4d77da8` — ячейка профиля верхом колонки (size-7 по центру над коллегами).
- `c649503` — ободок `ring-1 ring-primary/60`; dwell-зона без профиля; z-[70] порталы.
- `f559f6a` — когерентный набор «Заметки» + сортировка + z-лестница + dwell-зона + инвалидация.
- `98a89d3` — ось отдельным слоем поверх непрозрачной панели (понадобится, ТОЛЬКО если владелец вернёт оверлейную панель вместо рефлоу).
